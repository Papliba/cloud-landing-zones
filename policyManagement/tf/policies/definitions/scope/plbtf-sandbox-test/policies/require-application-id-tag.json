{
  "name": "require-application-id-tag",

  "properties": {
    "displayName": "Require Application ID Tag on All Resources",
    "description": "This policy requires that all resources have an application-id tag for tracking which application or service they belong to.",

    "mode": "Indexed",

    "metadata": {
      "category": "Tagging",
      "version": "1.0.0"
    },

    "parameters": {
      "effect": {
        "type": "String",
        "metadata": {
          "displayName": "Effect",
          "description": "Enable or disable the execution of the policy"
        },
        "allowedValues": ["Audit", "Deny", "Disabled"],
        "defaultValue": "Audit"
      }
    },

    "policyRule": {
      "if": {
        "anyOf": [
          {
            "field": "tags['application-id']",
            "exists": false
          },
          {
            "field": "tags['application-id']",
            "equals": ""
          }
        ]
      },
      "then": {
        "effect": "[parameters('effect')]"
      }
    }
  }
}
