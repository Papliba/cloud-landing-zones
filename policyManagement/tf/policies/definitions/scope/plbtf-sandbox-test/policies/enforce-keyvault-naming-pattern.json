{
  "name": "enforce-keyvault-naming-pattern",

  "properties": {
    "displayName": "Enforce Key Vault Naming Pattern",
    "description": "This policy enforces naming conventions for Azure Key Vaults with organization prefix, environment, and purpose identifiers.",

    "mode": "All",

    "metadata": {
      "category": "Naming Convention",
      "version": "1.0.0"
    },

    "parameters": {
      "effect": {
        "type": "String",
        "metadata": {
          "displayName": "Effect",
          "description": "Enable or disable the execution of the policy"
        },
        "allowedValues": ["Deny", "Audit", "Disabled"],
        "defaultValue": "Deny"
      }
    },

    "policyRule": {
      "if": {
        "allOf": [
          {
            "field": "type",
            "equals": "Microsoft.KeyVault/vaults"
          },
          {
            "not": {
              "field": "name",
              "match": "^plb-kv-(dev|test|prod)-[a-z0-9]+-[0-9]{3}$"
            }
          }
        ]
      },
      "then": {
        "effect": "[parameters('effect')]"
      }
    }
  }
}
