trigger:
  branches:
    include:
    - main

pool: 'selfhosted'

stages:
  - stage: mg_structure
    displayName: mg_structure
    jobs:
    - job: mg_create
      displayName: mg_create
      steps: 
      - script: echo "Hello World"
        displayName: "Print Hello World"
